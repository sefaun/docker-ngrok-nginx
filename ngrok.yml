version: 2
authtoken: <authtoken>
log: stdout
log_level: info
tunnels:
  api:
    addr: nginx:80
    proto: http
# docker çalıştıktan sonra loglarda verilen domaini al. docker durdur. sonra alttaki satırı yaz ve tekrar başlat.
#    hostname: sefa.ngrok-free.dev
